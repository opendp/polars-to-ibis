# After making changes here, run scripts/requirements.py.

# End users only need the backend extra they'll be connecting to:
ibis-framework[duckdb,polars,sqlite,postgres,mysql]>=10.0.0

# Developer tools:
# pip-tools depends on particular versions of pip, so pin both.
# See: https://github.com/jazzband/pip-tools/issues/2252
pip-tools==7.5.3
pip==25.3
flit
flake8
flake8-bugbear
pre-commit
tomlkit
isort
black
pyyaml

# Testing:
pytest
pyright
pytest-cov

# OpenDP is not necessary at runtime,
# but it is used in tests as an example of a Polars plugin.
opendp[polars]>=0.14.1

# Package dependencies:
-r requirements.in
